
# AES-256 key for feedback attachments, 64 hex characters (01-DESIGN §16b).
# Generate with: openssl rand -hex 32
# When absent the API REFUSES attachments rather than storing them in the
# clear — a refusal is visible, an unencrypted write is not.
ATTACHMENT_KEY=
