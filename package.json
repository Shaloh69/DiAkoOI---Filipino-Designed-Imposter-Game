{
  "name": "diakooi",
  "private": true,
  "description": "DiAkoOi monorepo — Astro site, Next.js admin console, Fastify API, Playwright E2E. The Flutter app in /app is not a workspace member.",
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "build": "pnpm -r --filter=./site --filter=./admin --filter=./api build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
